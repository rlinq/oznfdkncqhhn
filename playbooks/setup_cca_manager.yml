# Configure the manager server
# Run the playbook with
# ansible-playbook -i HOST_IP, setup_cca_manager.yml
- hosts: all
  gather_facts: 'no'
  serial: 1
  pre_tasks:
    - setup:
        gather_subset: 'network'
    - include_role:
        name: ./roles/cca.setup.cca-manager
      when:
        - not cca_docker_mount_dir is defined

    - include_role:
        name: ./roles/cca.setup.cca-manager
        tasks_from:
          pre_configure_cca_manager_user.yml
      when:
        - cca_docker_mount_dir is defined
